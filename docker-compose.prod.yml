services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: origo-front
    restart: unless-stopped
    cap_add:
      - NET_BIND_SERVICE
    ports:
      - "9000:80"
